import styled from "styled-components";

const Main = styled.main`
    padding-top: 25px;
    form {
        margin-top: 40px;
        display: flex;
        flex-direction: column;
    }
`;

export default Main;